{
  "name": "@vector-space/desktop",
  "private": true,
  "version": "0.1.0",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "concurrently -k -n RENDERER,MAIN,ELECTRON \"npm run dev:renderer\" \"npm run dev:main\" \"npm run dev:electron\"",
    "dev:renderer": "vite",
    "dev:main": "tsc -p tsconfig.main.json --watch --preserveWatchOutput",
    "dev:electron": "cross-env-shell VITE_DEV_SERVER_URL=http://localhost:5173 \"wait-on tcp:5173 dist/main/index.js && electronmon dist/main/index.js\"",
    "build": "npm run build:main && npm run build:renderer",
    "build:main": "tsc -p tsconfig.main.json",
    "build:renderer": "vite build",
    "typecheck": "tsc -p tsconfig.main.json --noEmit && tsc -p tsconfig.renderer.json --noEmit",
    "package:mac": "npm run build && electron-packager . \"Vector Space\" --platform=darwin --arch=x64 --out=release --overwrite --app-bundle-id=com.vectorspace.app"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "keytar": "^7.9.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron": "^35.0.0",
    "electron-packager": "^17.1.2",
    "electronmon": "^2.0.3",
    "vite": "^6.2.0",
    "wait-on": "^8.0.3"
  }
}
